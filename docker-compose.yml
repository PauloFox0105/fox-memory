services:
  fox-memory:
    build: .
    container_name: fox-memory
    restart: unless-stopped
    ports:
      - "18820:18810"
    volumes:
      - ./memory.db:/app/memory.db
    env_file:
      - .env
